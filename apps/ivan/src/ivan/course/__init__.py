"""
Course / time-trial helpers.

This is intentionally small and local-only for now:
- course volumes can be defined in map.json (future) or via local overrides
- runs are timed locally and persisted in the user's state file
"""

